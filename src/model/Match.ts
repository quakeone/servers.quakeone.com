export type Match = {
  gameId: number,
  matchId: number,
  serverId: number,
  hostName: string,
  serverName: string,
  map: string,
  mod: string,
  matchStart: string,
  matchEnd: string,
  matchDuration: string
}
