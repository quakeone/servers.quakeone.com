export type MatchPLayer = {
  aliasBytes64 : string,
  playerId: number,
  playerStayDuration: number,
  playerMatchStart: number,
  playerMatchEnd: number,
  skin: string,
  model: string,
  pantColor: string,
  shirtColor: string,
  frags: number
}